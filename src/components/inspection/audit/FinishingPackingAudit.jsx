import AuditSection from "./AuditSection";

const FinishingPackingAudit = () => {
  // The 'mainTitle' prop must match the value in your database for this section.
  return <AuditSection mainTitle="Finishing and Packing" />;
};

export default FinishingPackingAudit;
